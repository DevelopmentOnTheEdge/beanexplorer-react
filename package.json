{
  "name": "beanexplorer-react-dps",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/DevelopmentOnTheEdge/beanexplorer-react-dps",
  "dependencies": {
    "json-pointer": "^0.6.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "coverage": "npm test -- --coverage",
    "coveralls": "npm run-script coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "eject": "react-scripts eject"
  }
}
